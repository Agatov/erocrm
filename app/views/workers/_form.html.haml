= simple_form_for worker, url: url do |f|
  = f.input :name
  = f.input :post, collection: Worker.posts_for_select, as: :radio_buttons, wrapper: :vertical_radio_and_checkboxes
  = f.input :discharged
  = f.button :submit
