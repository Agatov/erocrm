%nav.navbar.navbar-default{role: :navigation}
  .container
    .navbar-header
      %button.navbar-toggle{type: :button, 'data-toggle' => 'collapse'}
        %span.sr-only
          Toggle Navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: root_path}
        CRM

    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li{class: params[:controller] == 'visits' ? 'active' : ''}
          %a{href: visits_path}
            Клиенты

        %li{class: params[:controller] == 'programs' ? 'active' : ''}
          %a{href: programs_path}
            Программы

        %li{class: params[:controller] == 'workers' ? 'active' : ''}
          %a{href: workers_path}
            Сотрудники

      %form.navbar-form.navbar-right
        %a.btn.btn-success{href: new_visit_path}
          Добавить клиента